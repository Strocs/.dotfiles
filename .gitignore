# Local Pi runtime state
.atl/

# Symlinks (stow targets, not repo source)
/.config

# Pi-agent runtime state (LMDB frecency/history)
pi-agent/.pi/agent/pi-pretty/

# Local backup files
/gemini/.gemini/settings.json.orig
